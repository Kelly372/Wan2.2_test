# 画质基线实验 Q0–Q3

已有实验中，Q0 对应 `A_reconstruction.mp4`（原视频的 VAE 重建），Q1 对应 `origin_minus_diff_w0.mp4`（原视频加噪、无残差注入的去噪）。此前观察为 Q0 清晰、Q1 物体边缘模糊抖动，因此下一步检查加噪幅度和持续首帧约束。

## 运行

需要仓库完整推理依赖、CUDA GPU 和完整的 Wan2.2-TI2V-5B 模型目录。VAE 从模型目录中的 `Wan2.2_VAE.pth` 加载。

```bash
python run_sigma_video.py --model_path /path/to/Wan2.2-TI2V-5B --video_tag xxx
python run_i2v_clean.py --model_path /path/to/Wan2.2-TI2V-5B --video_tag xxx
```

两个脚本均从 `data/video/xxx.mp4` 重新生成 `xxx_lowResolution.mp4` 和 `xxx_first_frame.mp4`，统一为 H=384、W=672，保留原视频帧数与 FPS。内部帧数补齐到 4n+1，输出裁回原帧数。结果存入 `data/video/xxx/`，采用 H.264/yuv420p MP4。

默认 `--inference_step 25 --seed 42 --prompt "" --device_id 0`；可以直接通过参数修改。CFG=5、shift=5，与此前基线相同。空提示词用于复现旧基线；若改为场景描述，应在所有待比较实验中使用同一描述。脚本复用 `run_diff_video.py` 的视频和推理工具，不执行旧残差实验，也不修改旧脚本。

## Q2：实际起始 σ

初始 latent 为 `(1-σ) * E(A) + σ * ε`，四组共享同一个原视频 latent、随机噪声、提示词和模型。无残差注入，无持续首帧约束。

| 输出 | 实验 |
| --- | --- |
| `sigma_0.2.mp4` | 起始 σ=0.2 |
| `sigma_0.4.mp4` | 起始 σ=0.4 |
| `sigma_0.6.mp4` | 起始 σ=0.6 |
| `sigma_reference.mp4` | 旧 Q1 的起始 σ，约 0.833055 |

每组默认执行 **25 次真实去噪更新**，最终 σ=0。自定义 σ 先反解 shift，再构造调度，避免把 σ=0.2 再经 shift 变成更高的实际噪声。reference 在默认 25 步时直接使用旧 50 步调度的后 25 步，保留原整数 timestep，作为复现检查；如果修改步数，则保持其起始 σ 并重新分配更新。

`sigma_metadata.json` 记录每组实际起始 σ、完整有效 timestep/σ 序列、提示词、seed 等。硬件和推理内核差异仍可能影响逐像素复现。

重点比较物体边缘、闪烁、纹理和视角运动是否保留。较低 σ 若改善画质，说明强加噪/再生成是损失来源之一；它同时保留了更多源视频信息，不能据此认定已经实现独立的视角控制。

## Q3：原生 I2V 持续干净首帧约束

| 输出 | 实验 |
| --- | --- |
| `i2v_clean.mp4` | 调用原生 `WanTI2V.i2v`：编码首张图像，将第一时间位置的 latent 固定为干净特征，相关 token 的 timestep 设为 0，并在每次调度器更新后恢复该特征；其余位置从随机噪声开始 |
| `i2v_no_anchor.mp4` | 调用原生 `WanTI2V.t2v`：相同 seed、latent 形状、提示词、调度及更新数，所有位置从随机噪声开始，无首帧条件 |

两组均使用原生完整噪声调度，默认 25 次更新；不是 Q2 的原视频加噪初始化。干净约束指 latent 条件，不保证解码后的第一帧与输入图像逐像素一致。

`i2v_clean_metadata.json` 记录采样参数、补齐帧数、调度和已生成文件。重点检查 I2V 是否能维持首帧外观、边缘与跨帧稳定性。无首帧对照可能生成不同内容；它用于观察加入首帧条件的整体影响，不能作为严格同内容的像素质量对照。Q3 与 Q2 的初始化也不同，二者画质差异不能全部归因于首帧约束。

Q2/Q3 都没有输入相机参数，不要求生成视频复现指定轨迹。它们用于确定后续视角控制实验的稳定画质基线。选出候选设置后，建议在同一组视频、相同提示词下用多个 seed 复核，避免只根据单次结果作结论。

本地 CPU 检查覆盖调度构造、原生分支调用参数、帧数补齐及现有视频保存回归；实际显存占用和生成画质仍需在有模型的 Linux/CUDA 环境验证。
