# run_replace_video.py 结果说明

```bash
python run_replace_video.py --model_path /path/to/Wan2.2-TI2V-5B --video_tag clip
```

该脚本复用 run_diff_video.py 中的预处理和视频保存函数，先生成低分辨率 A 和首帧视频 B，再分别以 `(1-σ)*E(A/B)+σ*ε` 初始化去噪；两条轨迹共享同一份随机噪声、seed=42、空提示词及官方负面提示词，不添加残差。沿用之前 50 步 UniPC 日程的后 25 步（strength=0.5），实际执行 25 次更新，初始 σ 取日程第 26 项，并不直接等于 0.5。

先完整运行不受干预的 Process(A)、Process(B) 基线，保存全部 25 次更新前及最终 latent 快照。之后独立运行双向交换实验，供体始终从同步的、未受干预的基线快照重新计算；持续替换时每一步都会使用该步的新供体特征，不重复使用窗口起点的特征。

## 新实验及命名

默认运行全部实验，也可加 `--experiment_set single`、`--experiment_set window` 或 `--experiment_set full` 只运行一类；每类都会重新计算两条基线。

全部新增结果保存在 `data/video/clip/attention_extended/`，原目录中的 `swap_*_step*.mp4` 和原有元数据不覆盖。以下 `updates` 是 **从 1 开始的实际去噪更新序号，区间包含两端**，不同于旧文件名中的剩余步数 `step`。

| 文件示例（A 接收 B） | 交换发生在第几次去噪更新 |
| --- | --- |
| `single_swap_A_from_B_updates01-01.mp4` | 单次更新；五组分别为 1、6、11、16、21 |
| `window_swap_A_from_B_updates01-05.mp4` | 连续窗口；五组分别为 1–5、6–10、11–15、16–20、21–25 |
| `full_swap_A_from_B_updates01-25.mp4` | 全部 25 次更新 |

B 接收 A 对应 `*_swap_B_from_A_updates*.mp4`。默认共 22 组替换视频，加 `extended_process_A_baseline.mp4`、`extended_process_B_baseline.mp4`，合计 24 个视频。

每个指定更新中替换所有 30 层（代码索引 0–29）的 `block.self_attn` 输出，即输出投影后、接收方门控与残差相加前的特征。CFG 的条件分支接收供体条件特征，无条件分支接收供体无条件特征。接收方的 latent、残差连接、cross-attention、FFN 和 UniPC 历史均保留；窗口外恢复正常计算。每组始终完成 25 次更新。

## 诊断记录

每个替换视频对应一个同名前缀的 `_diagnostics.json`，按时间顺序记录全部 25 次更新：

| 字段 | 比较内容 |
| --- | --- |
| `conditional_prediction` / `unconditional_prediction` | 在同一个接收方输入 latent 上，替换与不替换的模型输出差异；仅干预步记录 |
| `cfg_prediction` | 上述两组输出分别合并 CFG 后的预测差异；仅干预步记录 |
| `local_next_latent` | 同一个输入、同一份求解器历史下，替换与不替换执行一次更新后的 latent 差异；仅干预步记录 |
| `local_update` | 同上，但先减去更新前 latent；相对误差以正常更新量为基准，避免被 latent 本身的幅度掩盖 |
| `trajectory_vs_recipient_baseline` | 当前实验更新后的 latent 与原始接收方基线同一步输出的差异；包括干预结束后的所有步骤 |

每项包含 RMSE、最大绝对差、参考张量 RMS、相对 L2。相对 L2 的分母为 `max(reference_rms, 1e-12)`，参考幅度接近零时需结合绝对误差判断，不能只看比例。

局部“不替换”对照是在**当前已受前序干预的接收方状态**上计算，不是拿原始基线预测代替。代码复制更新前的完整 UniPC 状态，在副本上试算不替换的单步结果；真实求解器只推进一次，诊断不改变实验轨迹。累计轨迹差异则单独与原始基线比较。

`attention_extended_metadata.json` 记录本次选择的实验集、更新序号、实际 timestep/σ、日程、诊断定义和自身替换检查结果。每组诊断在完成去噪后写入，视频在全部选中轨迹完成后统一解码。对同一个实验集重跑会覆盖该扩展目录中相应的新结果。

如果预测差异明显，但局部更新差异很小，说明变化传递到更新后的幅度较小；若累计差异在窗口结束后回落，才支持后续去噪减弱干预的解释。latent 差异大也不等于运动发生转移，需要同时比较视频运动、边缘清晰度和闪烁。

## 验证和成本

两条基线在五个单步交换点、两个 CFG 分支都会额外执行“自己的特征替换自己”的检查，比较完整模型输出（rtol/atol 均为 1e-4）。检查失败会终止实验。供体特征按层临时写入结果目录下的缓存，交换时逐层读取，每次只保存一个 CFG 分支的特征，用完清理。

全套包含 24 条各 25 步轨迹；每个干预步还有供体计算、正常预测对照和求解器副本试算。全部基线快照和最终 latent 保存在 CPU，诊断归约也在 CPU 上进行；比旧单步实验需要更多运行时间、内存和临时磁盘读写。CPU 测试覆盖连续替换范围、供体同步、两个 CFG 分支、诊断不改变真实轨迹和求解器历史；实际 GPU 推理和画质需在有模型的环境验证。

这些结果用于观察全层注意力干预在哪个时间阶段更容易改变布局，不直接证明存在唯一决定布局的 timestep。全层替换是强干预，伪影、时间一致性变化和接收方多步求解器的历史响应也应一并观察。
