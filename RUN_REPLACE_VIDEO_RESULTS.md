# run_replace_video.py 结果说明

```bash
python run_replace_video.py --model_path /path/to/Wan2.2-TI2V-5B --video_tag clip
```

该脚本复用 run_diff_video.py 中的预处理和视频保存函数，先生成低分辨率 A 和首帧视频 B，再分别以 `(1-σ)*E(A/B)+σ*ε` 初始化去噪；两条轨迹共享同一份随机噪声、seed=42、空提示词及官方负面提示词，不添加残差。沿用之前 50 步 UniPC 日程的后 25 步（strength=0.5），实际执行 25 次更新，初始 σ 取日程第 26 项，并不直接等于 0.5。

先完整运行不受干预的 Process(A)、Process(B) 基线，并保存五个时刻的输入 latent 快照。之后独立运行双向交换实验，供体始终从对应时刻的基线快照重新计算，避免使用已被交换污染的特征。

| 文件名中的 step | 交换发生在第几次去噪更新 |
| --- | --- |
| 25 | 1 |
| 20 | 6 |
| 15 | 11 |
| 10 | 16 |
| 5 | 21 |

每组只在该次更新中替换一次所有 30 层（代码索引 0–29）的 `block.self_attn` 输出，即输出投影后、接收方门控与残差相加前的特征。CFG 的条件分支接收供体条件特征，无条件分支接收供体无条件特征。接收方的 latent、残差连接、cross-attention、FFN 和 UniPC 历史均保留，后续更新恢复正常计算。

结果保存在 `data/video/clip/`：

- `process_A_baseline.mp4`、`process_B_baseline.mp4`：未交换的两条基线。
- `swap_A_from_B_step25.mp4` 等五个视频：A 接收 B，step 为 25、20、15、10、5。
- `swap_B_from_A_step25.mp4` 等五个视频：B 接收 A，同样五个时刻。
- `attention_swap_metadata.json`：交换方向、实际 timestep/σ、层范围、日程和自身替换检查结果。

两条基线在五个交换点、两个 CFG 分支都会额外执行“自己的特征替换自己”的检查，比较完整模型输出（rtol/atol 均为 1e-4）。检查失败会终止实验，避免把实现或数值不一致当作交换效果。供体特征按层临时写入结果目录下的缓存，交换时逐层读取，每次只保存一个 CFG 分支的特征，用完清理；需要额外磁盘空间和读写时间。全套包含 12 条各 25 步轨迹及额外供体计算，比原先两路推理耗时更多。

这些结果用于观察全层注意力干预在哪个时间阶段更容易改变布局，不直接证明存在唯一决定布局的 timestep。全层替换是强干预，伪影、时间一致性变化和接收方多步求解器的历史响应也应一并观察。
